<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- A suíte renderiza cerca de 25 PDFs e decodifica o acervo de
             figuras do nível 3 num único processo: o dompdf e o GD seguram
             memória entre um teste e outro, e os 128 MB padrão do CLI acabam
             no meio do caminho. Não é vazamento, é volume. -->
        <ini name="memory_limit" value="512M"/>
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="saap_local_test"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <!-- Sem chave, o resumo por IA fica DESLIGADO na suíte inteira.
             Sem isto os testes herdam a chave real do .env e, como a fila roda
             em 'sync', todo teste que conclui uma avaliação dispara o job do
             laudo e chama o Google de verdade: dinheiro, lentidão e dado de
             teste saindo para terceiro. Quem testa o recurso liga a chave no
             próprio teste, junto com Http::fake(). -->
        <env name="GEMINI_API_KEY" value=""/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
